[package]
name = "xdb-node"
version = "0.3.5"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", default-features = false, features = ["napi4"] }
napi-derive = "2.16"
x-db = { path = "../.." }

[build-dependencies]
napi-build = "2.1"
