[package]
name = "xdb-cli"
version = "0.3.5"
edition = "2021"
description = "CLI tools สำหรับ x-db: check / stats / dump / get / merge"

[[bin]]
name = "xdb"
path = "src/main.rs"

[dependencies]
x-db = { path = "../.." }
